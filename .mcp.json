{
  "mcpServers": {
    "Salesforce DX": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@salesforce/mcp", "--orgs", "dev1", "--toolsets", "orgs,metadata,data,users"],
      "env": {}
    }
  }
}
