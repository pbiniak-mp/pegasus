<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Quote_Type_Change</fullName>
    <active>true</active>
    <description>Quote Type drives the Opportunity amount aggregation and the active-Quote uniqueness rules, so it must be fixed once the Quote is created.</description>
    <errorConditionFormula>AND(NOT(ISNEW()), ISCHANGED(Quote_Type__c))</errorConditionFormula>
    <errorDisplayField>Quote_Type__c</errorDisplayField>
    <errorMessage>Quote Type cannot be changed after the record has been created.</errorMessage>
</ValidationRule>
